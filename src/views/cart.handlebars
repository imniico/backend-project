<div>
    <h1> Carrito - ID:{{cart._id}}</h1>

    <ul>
        {{#each cart.products}}
            <li>
                Producto: {{this.product.title}} - Cantidad: {{this.quantity}}
            </li>
        {{/each}}
    </ul>
</div>