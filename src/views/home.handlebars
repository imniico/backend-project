<div>
    <h1> Productos </h1>
    <ul>
        {{#each products.docs}}
            <li> ID:{{_id}} - Titulo:{{title}} - Precio:{{price}} - Stock: {{stock}} - Code: {{code}} </li>
        {{/each}}
    </ul>
</div>